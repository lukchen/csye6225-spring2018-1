version: 0.0
os: linux

files:             
  - source: /
    destination: /home/ubuntu  

hooks:
  AfterInstall:
    - location: scripts/startapplication.sh
      runas: root
      timeout: 300
  ApplicationStart:
    - location: 
      runas: root
      timeout: 60    